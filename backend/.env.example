# Environment variables
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True

# Optional: enable ONNX -> RDK .bin conversion on the server.
# `hb_mapper makert` typically runs inside Horizon OE Docker on your PC/laptop.
# Provide the command with placeholders. Preferred format is a JSON array of args.
# Supported placeholders:
# - {job_dir} : host directory containing ONNX+BIN (recommended for docker -v)
# - {onnx_container} / {bin_container} : /workspace paths inside container
# - {onnx} / {bin} : host paths
#
# Example (edit image + flags for your project):
# HORIZON_BIN_COMPILE_COMMAND=[
#   "docker","run","--rm",
#   "-v","{job_dir}:/workspace",
#   "-w","/workspace",
#   "<horizon-oe-image>",
#   "hb_mapper","makert",
#   "--model-type","onnx",
#   "--input","{onnx_container}",
#   "--output","{bin_container}"
# ]
HORIZON_BIN_COMPILE_COMMAND=

# RDK X5 Device Configuration
RDK_IP=192.168.1.100
RDK_USERNAME=sunrise
RDK_PASSWORD=sunrise
